<?php $paymentIcons = $block->getPaymentIcons(); ?>
<?php if (count($paymentIcons) > 0): ?>
    <?php foreach ($paymentIcons as $paymentIcon): ?>
        <img class="reepay-payment-icon" src="<?= $escaper->escapeUrl($paymentIcon); ?>">
    <?php endforeach; ?>
<?php endif; ?>
